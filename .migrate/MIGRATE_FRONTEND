# Frontend Migration Trigger

This marker file triggers automatic frontend migration when committed.

## What happens:

1. GitHub Actions detects this file
2. Runs migration script automatically
3. Commits refactored code to your branch
4. Posts status comment on PR

## To trigger:

```bash
git add .migrate/
git commit -m "Trigger frontend migration"
git push origin your-branch-name