{
  "name": "omnibot-tests",
  "version": "1.0.0",
  "description": "OmniBot test suite",
  "type": "module",
  "scripts": {
    "test": "mocha test/*.test.js --timeout 10000",
    "test:structure": "mocha test/omnibot.test.js --grep 'Structure' --timeout 5000",
    "test:safety": "mocha test/omnibot.test.js --grep 'Safety' --timeout 5000"
  },
  "devDependencies": {
    "mocha": "^10.2.0",
    "chai": "^4.3.10"
  }
}
