name = "omnibot"
main = "src/index.js"
compatibility_date = "2024-01-01"

# CRITICAL: Enable ES modules format
# Without this, "export default" syntax will fail
[build]
command = ""

[env]
# Empty but required for module format

# Use ES modules format (not service worker)
workers_dev = true
